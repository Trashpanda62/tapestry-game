# Locked visual check

Steve’s locked JSON and the baked production HTML were compared at 375×667 and 1440×900.

- Both viewports expose `hero=documentary`, `palette=pasture-ochre`, `surface=painted-sign`, and `motion=lively` on the root document.
- The mobile hero keeps the real host/guest/animal frame, primary booking CTA, secondary stay CTA, and trust line visible without selector controls.
- The desktop hero preserves the documentary focal point and the same CTA hierarchy; no default/empty axis fallback appears.
- Production pages contain no preset panel, selector script, hash persistence, or preview route.

