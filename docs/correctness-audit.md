# Correctness audit — S13/G1

Date: 2026-07-21 · artifact: `dist/` generated by `npm run build`.

## Coverage matrix

| Surface | Inputs / branches checked | Evidence |
|---|---|---|
| Build + release | `src/build-config.json` base/origin, required source files, deterministic copy, locked attrs, canonical URL rewrite, generated inventories, immutable release/active pointer | `npm run verify`, `npm run test:design-lock`, Webstudio static-bundle Vitest |
| Routes + redirects | eight canonical route records, legacy `.html` redirects, branded 404/thanks, traversal rejection in live route | `npm run test:routes`; `test/staticBundle.test.ts` |
| Navigation + forms | same-origin internal-link rewrite, nav/mobile shell, form required fields/honeypot/rate limit, lead success redirect, UTM allowlist | `npm run test:headings`, `npm run test:states`, `npm run test:analytics`, Webstudio `test/leadGuard.test.ts` |
| Experiences + RV | seven records, Square vs inquiry fallback, timing filter, BYO/supplied distinction, sourced current rates and unknown-policy contact path | `npm run test:experiences`, `npm run test:checkout`, `npm run test:rv` |
| Catalog + Shop | hashed index→catalog/facets, 242 families/830 SKUs, stock/image/URL guards, search/sort/load-more/history/offline/duplicate/unicode/long-title fixtures, Square conversion ping | `npm run test:shop`, `npm run test:catalog-map`, `npm run test:catalog-adversarial` |
| Animals + Herd | eight animal groups, named/unnamed/for-sale branches, same-origin mirror, route/SW scope, checksum, cache/network fallback, reduced-motion/keyboard paths | `npm run test:animals`, `npm run test:animal-copy`, `npm run test:herd-mirror`, `npm run test:herd-parity`, `npm run test:herd-offline` |
| Conversion events | allowlisted names/fields, clamps, forbidden PII keys, same-origin/Fetch-Metadata guard, rate limit, tenant-scoped summary/export, 90-day retention, post-lead authority, repeat-beacon dedupe | Webstudio `test/conversionEvents.test.ts` + Vitest (19 targeted tests) |
| External/runtime dependencies | recursive runtime URL crawl; comments removed; first-party, Square, approved map/photo, and schema vocabulary classified | `npm run test:external` — zero unexpected dependencies |

## Findings

- No P0/P1 correctness defects remain in the exercised matrix.
- The build now rewrites retired GitHub canonical/OG/JSON-LD URLs to the explicit live origin and tenant base path; sitemap/robots are generated from the same config.
- The publisher validates slug, origin, manifests, SEO artifacts, and analytics endpoint before S3/Prisma initialization; invalid or missing activation configuration aborts without changing the active pointer.
- Runtime external calls are intentionally limited to Square checkout, Google directions, and explicitly approved photo sources; schema.org/W3C URLs are metadata vocabulary, not network dependencies.
