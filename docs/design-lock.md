# Tapestry Acres design lock

Locked combo received from Steve on 2026-07-21:

```json
{
  "hero": "documentary",
  "palette": "pasture-ochre",
  "typography": "sturdy-slab",
  "surface": "painted-sign",
  "illustration": "accent",
  "density": "comfortable",
  "motion": "lively",
  "nav": "rail"
}
```

The selector preview source remains retained at `design-preview.html` for audit history, but it is unpublished. Production HTML receives the exact `data-*` attributes at build time; selector assets and panel code are excluded from `dist`.

